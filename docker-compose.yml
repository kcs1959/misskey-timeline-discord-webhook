services:
  app:
    build: .
    env_file: .env
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'test', '-f', '/tmp/healthy']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
